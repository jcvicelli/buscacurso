<%= simple_form_for(@category_type) do |f| %>
  <%= render "category_types/shared/error_messages" %>

  <div class="form-group">
    <%= f.association :category %>
    <%= f.input :name %>
  </div>

  <br>
  <%= f.submit "Salvar", class: 'btn btn-primary' %>
<% end %>
