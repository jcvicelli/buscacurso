<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'BuscaCurso', root_path, class: 'navbar-brand' %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li><%= link_to raw('<span class="glyphicon glyphicon-home"></span> Home'),   root_path %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <form role="search" class="navbar-form navbar-left">
          <div class="form-group">
            <input type="text" placeholder="Buscar" class="form-control">
          </div>
        </form>
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <span class="glyphicon glyphicon-user"></span>
              <%= current_user.name %> <b class="caret"></b>
            </a>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to 'Minha Conta', edit_user_registration_path %></li>
              <li><%= link_to 'Empresas', companies_path %></li>
              <li class="divider"></li>
              <li><%= link_to raw('<span class="glyphicon glyphicon-log-out"></span> Log out'), destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to raw('<span class="glyphicon glyphicon-user"></span> Criar Conta'), new_user_registration_path %></li>
          <li><%= link_to raw('<span class="glyphicon glyphicon-log-in"></span> Log in'), new_user_session_path %></li>
          <li><%= link_to raw('<span class="glyphicon glyphicon-log-in"></span> Facebook Log in'), user_omniauth_authorize_path( :facebook ) %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
